name: Build i2pd OpenWrt APK

on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build i2pd (${{ matrix.arch }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

      matrix:
        arch:
          - arm_cortex-a7
          - arm_cortex-a9
          - arm_cortex-a15_neon-vfpv4
          - aarch64_cortex-a53
          - aarch64_cortex-a72
          - aarch64_generic
          - x86_64
          - i386_pentium4
          - mipsel_24kc
          - mips_mips32
          - mips64_octeonplus
          - riscv64_generic

        branch:
          - openwrt-25.12

    steps:
      - name: Checkout package feed
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Verify package
        run: |
          echo "Architecture: ${{ matrix.arch }}"
          echo "Branch: openwrt-25.12"
          echo
          grep -E '^PKG_(NAME|VERSION|RELEASE|SOURCE|SOURCE_SUBDIR|HASH):' \
            i2pd/Makefile

      - name: Build OpenWrt package
        id: build
        continue-on-error: true
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: ${{ matrix.arch }}-openwrt-25.12
          PACKAGES: i2pd
          NO_REFRESH_CHECK: 1
          BUILD_LOG: 1
          ARTIFACTS_DIR: ${{ github.workspace }}/artifacts

      - name: Show build error
        if: steps.build.outcome == 'failure'
        shell: bash
        run: |
          set +e

          echo "========================================"
          echo "i2pd BUILD FAILED"
          echo "ARCH: ${{ matrix.arch }}"
          echo "SDK:  ${{ matrix.arch }}-openwrt-25.12"
          echo "========================================"

          echo
          echo "Available logs:"
          find artifacts logs -type f -print 2>/dev/null | sort

          echo
          echo "========================================"
          echo "Build errors"
          echo "========================================"

          find artifacts logs -type f 2>/dev/null | while read -r LOG; do
            grep -nEi \
              'error:|fatal error:|CMake Error|FAILED|undefined reference|cannot find|No such file|not found|ERROR:' \
              "$LOG" 2>/dev/null | tail -n 50
          done

          echo
          echo "========================================"
          echo "Last lines of logs"
          echo "========================================"

          find artifacts logs -type f 2>/dev/null | while read -r LOG; do
            echo
            echo "----- $LOG -----"
            tail -n 100 "$LOG"
          done

          exit 1

      - name: Stop if build failed
        if: steps.build.outcome == 'failure'
        run: exit 1

      - name: Rename APK
        id: apk
        shell: bash
        run: |
          set -e

          APK="$(find bin -type f -name 'i2pd-*.apk' | head -n 1)"

          if [ -z "$APK" ]; then
            echo "ERROR: i2pd APK not found"
            find bin -type f -name '*.apk' | sort || true
            exit 1
          fi

          BASENAME="$(basename "$APK")"
          DIR="$(dirname "$APK")"

          NEW_NAME="${BASENAME%.apk}-${{ matrix.arch }}.apk"
          NEW_PATH="${DIR}/${NEW_NAME}"

          mv "$APK" "$NEW_PATH"

          echo "Original: $APK"
          echo "Renamed: $NEW_PATH"

          echo "path=$NEW_PATH" >> "$GITHUB_OUTPUT"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: i2pd-${{ matrix.arch }}-openwrt-25.12
          path: ${{ steps.apk.outputs.path }}
          if-no-files-found: error

      - name: Upload APK to Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: ${{ steps.apk.outputs.path }}
          fail_on_unmatched_files: true
          overwrite_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
